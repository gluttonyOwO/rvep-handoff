# r2-bridge runtime deps. Install with:
#   pip install --user --break-system-packages -r requirements.txt
#
# rclpy is provided by ROS2 Humble (sourced from /opt/ros/humble), NOT pip.
livekit==1.1.8
